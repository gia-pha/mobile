name: gia_pha_mobile
description: "Gia Pha mobile project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  nb_utils: ^7.1.4
  image_picker: ^1.1.2
  package_info: ^2.0.2
  cached_network_image: ^3.4.1
  url_launcher: ^6.3.1
  org_chart: ^4.2.2+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - images/newsBlog/
